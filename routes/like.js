// const router = require("express").Router();
// const {
//   addEmoji,
//   getTotalReactions,
//   getEmojiCounts,
//   selectEmoji,
//   removeEmoji,
//   getAllReactions
// } = require("../controller/like");

// // Adding emoji per post
// router.post("/:postId/reactions/add", addEmoji);

// // Counting the total number of reactions per post
// router.get("/:postId/reactions/total", getTotalReactions);

// // Counting number of each emoji per post
// router.get("/:postId/reactions/count", getEmojiCounts);

// // Emoji changing when selecting different emoji
// router.put("/:postId/reactions", selectEmoji);

// // Removing emoji when deselecting
// router.delete("/:postId/reactions", removeEmoji);

// // Displaying all emojis and respective post and user
// router.get("/", getAllReactions);

// module.exports = router;
